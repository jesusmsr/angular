import { FadeOnScrollDirective } from './fade-on-scroll.directive';

describe('FadeOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
